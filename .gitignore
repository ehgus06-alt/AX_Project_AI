# 1. 파이썬 가상환경 (방금 만드신 방)
# 이거 안 적으면 수만 개의 라이브러리 파일이 깃허브에 올라갑니다!
.venv/
venv/
env/

# 2. 파이썬 캐시 파일 (실행 시 자동 생성되는 찌꺼기 파일들)
__pycache__/
*.py[cod]
*$py.class

# 3. 데이터 및 모델 파일
# 나중에 주가 데이터를 저장하거나 AI 모델 가중치를 저장할 때, 용량이 너무 커서 오류가 나는 것을 방지합니다.
*.csv
*.xlsx
*.json
*.h5
*.pkl
*.pt
*.pth

# 4. 보안 및 환경 변수 (매우 중요★)
# 나중에 증권사 API 키 같은 것을 쓸 때 절대 깃허브에 노출되면 안 됩니다.
.env
secrets.json

# 5. 에디터 및 OS 설정 파일
.vscode/
.DS_Store
Thumbs.db

# 6. 주피터 노트북 체크포인트 (노트북 사용 시)
.ipynb_checkpoints/